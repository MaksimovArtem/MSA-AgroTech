# ADR-002: Архитектура платформы и Edge для управления фермой

Автор: Максимов Артем

## 1. Контекст

Описание решения, описанного в **ADR-001: Разработка вариантов решения**

Разрабатывается платформа управления фермами, включающая:
- сбор телеметрии с оборудования (датчики, исполнительные устройства)
- обработку видеопотоков (анализ состояния животных)
- агрегацию метрик
- генерацию уведомлений
- веб и мобильный интерфейсы

## 2. Требования

Требования к системе описаны в **ADR-001: Разработка вариантов решения**
В рамках этого документа требуется описать высокоуровневые контексты системы и взаимодействие между ними

Система должна:
- обрабатывать видеопотоки в реальном времени
- быть отказоустойчивой
- поддерживать асинхронную обработку событий
- работать в условиях нестабильной связи с фермами

## 3. Решение

Так как в заданиях 1-4 платформа разрабатывается для самой компании - можно использовать уже имеющийся у нее BI-модуль для агрегации и визуализации метрик. В рамках MVP и того, что явно это не обговорено, считается, что у него есть HTTPS интерфейс, способный получать метрики из внешних источников. До доработки этим интерфейсом мог пользоваться "Аналитик", работающий с BI-системой.

C2 диаграмма представлена в C2_solution.puml

Принято решение реализовать платформу и Edge по следующим признакам:

### 1. Микросервисная архитектура

На бэкенде выделены следующие контексты:
- Работа с пользователями
- Работа с животными
- Работа с устройствами
- Отправка агрегированных метрик на BI-модуль
- Взаимодействие с устройствами и телеметрией на ферме

Особенности реализации:
- Каждый сервис изолирован и имеет собственную базу данных
- Публикуются события аудита/метрики для BI и локальные алерты для персонала фермы
- Edge-логика обрабатывает критичные события на месте и агрегирует данные для платформы

### 2. Frontend

- Web: React / Vue
- Mobile: React Native

Причины:
- Высокая скорость разработки
- Переиспользование экспертизы внутри команды(у "АгроТех Х уже есть веб-приложение на React")
- React Native обеспечивает мультиплатформенность (iOS + Android)

SEO не является критическим, так как система ориентирована на авторизованных пользователей.

### 3. Основной backend стек — Go

Причины выбора:
- Высокая производительность
- Эффективная работа с конкурентностью (goroutines)
- Низкое потребление памяти
- Быстрый старт контейнеров
- Интеграция с Kubernetes
- Простой деплой (статический бинарник)

### 4. Синхронное взаимодействие через REST

Для синхронного взаимодействия между:
- Frontend -> API Gateway
- Edge -> API Gateway(для агрегированных метрик по ферме)
- API Gateway -> backend-сервисы

принят подход REST over HTTP/HTTPS.

Преимущества:
1. React / React Native нативно работают по HTTP
2. Простота разработки и тестирования
	- Читаемые URL
	- JSON
	- Легко тестировать (curl / Postman)
4. Низкий порог входа для разработчиков
5. Независимость клиентских приложений

### 5. Асинхронное взаимодействие через брокеры (RabbitMQ)

Используются:
- На каждом Edge:
	- Брокер для телеметрии оборудования (MQTT)
	- Брокер для передачи событий
		Могут быть одним физическим инстансом
- Брокер доменных событий (события по животным и агрегаты)

Причины:
- Снижение связности сервисов
- Нативная поддержка MQTT через плагин RabbitMQ
- Возможность масштабирования потребителей
- Устойчивость к временной недоступности сервисов
- Платформа не является сильно нагруженной, тк работает внутри "АгроТех Х". Edge шлют агрегаты метрик, тч также не генерируют большой поток событий для BI

### 6. Базы данных

- PostgreSQL для бизнес-данных (животные, устройства, локальные события)
- S3 для хранения видео(локально на ферме)

Причины:
- Соответствие отраслевым стандартам
- Отделение структурированных данных от медиа
- Масштабируемость

### 7. Edge Core / Edge Manager

Edge-устройства выполняют следующие функции:
- Обработка видеопотока (RTSP)
	- Анализ состояния животных на основе видеопотока
- Приём телеметрии с устройств(MQTT)
- Локальная генерация алертов (Push/SMS)
- Агрегация информации для платформы и отправка ее на платформу
- Применение политик с платформы

Причины:
- Локальная реакция на критичные события без зависимости от сети
- Обеспечение автономности фермы при нестабильном интернет-соединении
- Снижение нагрузки на платформу (передаются только агрегаты)

### 8. Взаимодействие Edge и платформы

Все критичные события (тревоги, аварии) обрабатываются локально на Edge и отправляются персоналу при необходимости. 

Платформа получает только **агрегаты и метрики** через REST API.
## 4. Альтернативы

### 1. Технология синхронного взаимодействия

gRPC / GraphQL отклонены из-за усложнения системы и отсутствия функциональной необходимости

### 2. Основной язык backend

Java не выбран потому что:
- лишний оверхэд для мелких сервисов из-за JVM.
- более сложное управление зависимостями и сборкой в микросервисной среде по сравнению с Go.


Python не выбран потому что:
- меньшая производительность и выше latency для сетевых сервисов.
- управление зависимостями и деплой более проблематично  по сравнению с Go.

### 3. Edge-логика

Полная передача событий на платформу отклонена: слишком высокая нагрузка и зависимость от нестабильной сети