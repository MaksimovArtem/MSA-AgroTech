Из контекста задания это неясно, но при обсуждении в пачке сообщили, что надо сделать 1-2 диаграммы C3 и C4 для самых сложных контейнеров и компонентов: https://app.pachca.com/chats/34097893?message=789904952

Для Solution:
- разобрал на C3 контейнер edge_manager, реализующий центральную логику Edge-компонента
- разобрал на С4 компонент service_layer из edge_manager

В C3 диаграмме edge_manager RabbitMQ брокер(не инстанс работающий на MQTT от датчиков оборудования) целенаправленно разделен на 2 контейнера для улучшения отображения на диаграмме. Реально это может быть один инстанс(и таким он отображен на диаграмме С2)

===

Для Alter:
- разобрал на C3 контейнер edge_gateway, который принимает данные с датчиков и камер
- разобрал на С3 контейнер video_service, который получает видеопоток и обрабатывает его с помощью ML
- разобрал на С4 компонент ml_processor из контейнера video_service

В C3 диаграмме edge_gateway RabbitMQ брокер(инстанс работающий на MQTT от датчиков оборудования) целенаправленно разделен на 2 контейнера для улучшения отображения на диаграмме. Реально это один инстанс(и таким он отображен на диаграмме С2)ы